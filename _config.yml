title: PKU_VLSI_Wiki
lang: en
description: PKU VLSI Wiki

remote_theme: rundocs/jekyll-rtd-theme

readme_index:
  with_frontmatter: true

exclude:
  - Makefile
  - CNAME
  - Gemfile
  - Gemfile.lock
  
  
  plugins:
  - jekyll-auth

jekyll_auth:
  users:
    - username: vlsi
      password: a219
  exclude:
    - /exclude-this-path/*
